# Render: repo kökü = priceplay, API = server/ alt klasörü
services:
  - type: web
    name: priceplay-api
    runtime: node
    plan: free
    rootDir: server
    buildCommand: npm install
    startCommand: npm start
